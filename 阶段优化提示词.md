# DeepSeek Pet 优化路线图 · 分阶段提示词

> 用法：每个阶段是一段完整、可直接投喂给 AI 的提示词。阶段之间有依赖关系，请按顺序执行；每阶段完成后人工确认再进入下一阶段。
>
> 通用约束（已写入每段提示词）：
>
> - 项目路径：`C:\Users\kgnb666.DESKTOP-55DCFH3.003\Desktop\DeepSeekPet`
> - 技术栈保持 Electron + 原生 JS，不重写、不换框架
> - 不更改 `assets/` 下四张角色立绘与整体 UI 框架
> - 不提前实现后续阶段的功能
> - 每阶段完成后汇报：①修改了哪些文件 ②如何运行验证 ③完成了什么 ④尚未实现什么

---

## 阶段一：稳定性修复（Bug Fix）

```text
你现在是 DeepSeek Pet 桌面宠物项目的主程。项目位于 C:\Users\kgnb666.DESKTOP-55DCFH3.003\Desktop\DeepSeekPet，技术栈 Electron + 原生 JS（main.js / preload.js / renderer.js / index.html / style.css）。

硬约束：
- 不更改 assets/ 下四张角色立绘，不更改整体 UI 框架与配色
- 只修 bug 与补齐缺失的基础能力，不做新功能
- 不提前实现后续阶段功能

必须修复的问题（均已在代码中确认存在）：
1. renderer.js：面板显示状态 settings.panelOpen 只读不写。「收起面板」按钮和右键菜单「打开面板」切换后没有调用 saveSettings 持久化，重启后状态丢失。修复为每次切换都保存。
2. main.js：缺少单实例锁。重复启动会开出两只宠物。用 app.requestSingleInstanceLock() 实现，第二次启动时聚焦已有窗口并退出新实例。
3. main.js：开机启动参数错误。app.setLoginItemSettings 无条件传 args: [app.getAppPath()]，这在打包后的便携版 exe 上是错误行为。改为：仅开发环境（!app.isPackaged）传 args，打包环境只传 path: process.execPath。
4. main.js：窗口位置不保存。用户拖动后退出，重启回到固定右下角。要求：拖动结束（防抖 1s）与退出前保存窗口 bounds 到 settings.json，启动时恢复；恢复时必须 clamp 到当前显示器 workArea 内（防止拔掉副屏后窗口出现在不可见区域）。
5. main.js：托盘菜单过于简陋。补全为：打开面板 / 显示隐藏 / 迷你模式切换 / 暂停动画开关（先发 IPC 给渲染层，渲染层收到后冻结所有 CSS 动画与定时气泡）/ 退出。
6. renderer.js：低价时段切换没有主动提醒。当前只有启动时提示一次。要求：每秒的 tickPeriod 检测 isLow 跳变，进入低价时段弹绿色气泡「低价时段开始啦」，进入高价时段弹普通气泡提醒，且切换宠物姿势（低价 char_happy 短暂展示，高价 char_sleep 短暂展示）。
7. main.js：fetch-balance 失败时静默退回演示数据，渲染层无法区分。把 error 字段展示到面板 demo-badge 上（如「演示数据·连接失败」），让用户知道真实余额没查到。

完成后：
- 用 electron . 启动验证：拖动重启位置恢复、双开被拦截、面板状态持久化、托盘新菜单可用、时段切换提醒可用（可将系统时间或 OFFPEAK 常量临时改到临近时间测试，测完恢复）
- 汇报：①修改了哪些文件 ②如何运行验证 ③完成了什么 ④尚未实现什么
不要为了完成任务而降低代码质量。
```

---

## 阶段二：体验增强（宠物"活"起来）

```text
你现在是 DeepSeek Pet 桌面宠物项目的主程。项目位于 C:\Users\kgnb666.DESKTOP-55DCFH3.003\Desktop\DeepSeekPet，技术栈 Electron + 原生 JS。阶段一（稳定性修复）已完成。

硬约束：
- 不更改 assets/ 下四张角色立绘（char_main/happy/work/sleep.png），动画只能用 CSS/Transform/透明PNG切换实现，不引入新人物素材
- 不更改面板整体布局与配色
- 不提前实现阶段三、四的功能

必须实现：
1. 暂停动画：渲染层实现全局动画开关（暂停所有 CSS animation、定时气泡、宠物自动行为），与托盘菜单的暂停项联动（阶段一已留 IPC），状态保存到 settings.json，右键菜单加「暂停动画」勾选项。
2. 空闲随机行为：无操作 60s 后进入 idle 循环，每 30-90s 随机触发一个行为：随机台词气泡（台词池至少 10 条，含时间感知：深夜提醒休息）、打盹（char_sleep 5s）、卖萌（char_happy + 爱心）、伸懒腰（CSS scale 拉伸）。任意鼠标交互立即打断回 idle。
3. 眨眼动画：不修改立绘的前提下，用 CSS 在宠物眼睛区域叠加两个伪元素或用 Web Animations 对整体做极轻微 scaleY(0.98→1) 模拟，每 3-6s 随机眨眼一次。效果不好就换方案，禁止生造新人物图。
4. 拖动体验：当前 document 级 mousemove 拖出窗口会断。改用 pointerdown + setPointerCapture 重写拖动逻辑；迷你条同样适用。
5. 气泡队列：多个气泡事件连续触发时排队展示（每个至少展示 3s），不要互相覆盖丢失。
6. 多显示器支持：窗口拖动到副屏后，迷你模式 clamp 应使用窗口所在显示器的 workArea（screen.getDisplayMatching），而不是主屏。
7. 资源瘦身：assets/ 四张 PNG（各约 1.5MB）用 PIL 等比缩到高 600px 并另存一份无损 WebP，渲染层优先加载 WebP（<picture> 或 JS 检测），PNG 保留为回退。包体应明显变小。

完成后：
- 启动验证：暂停/恢复、空闲行为触发、拖出窗口不断、气泡排队、副屏拖放+迷你模式、exe 重新打包后体积对比
- 汇报：①修改了哪些文件 ②如何运行验证 ③完成了什么 ④尚未实现什么
不要为了完成任务而降低代码质量。
```

---

## 阶段三：数据真实化（DeepSeek API 正式接入）

```text
你现在是 DeepSeek Pet 桌面宠物项目的主程。项目位于 C:\Users\kgnb666.DESKTOP-55DCFH3.003\Desktop\DeepSeekPet，技术栈 Electron + 原生 JS。阶段一、二已完成。

硬约束：
- 不更改角色形象与整体 UI 框架
- 所有网络请求失败都必须有明确的 UI 反馈，禁止静默降级
- 不提前实现阶段四功能

必须实现：
1. API Key 安全存储：改用 Electron safeStorage 加密 API Key 后落盘（settings.json 存密文），读取时解密；safeStorage 不可用时降级明文并在设置页提示。已存在的明文 Key 自动迁移。
2. API 错误分类反馈：401/403 提示「Key 无效」、429 提示「请求过频」、超时/断网提示「网络异常」，在面板头部以徽标形式展示，并弹一次警告气泡。
3. 价格表外置：把 PRICES 常量抽到项目根 prices.json，渲染层启动时经 IPC 从主进程读取；文件缺失时用内置默认值并提示。价格表需含 deepseek-v4-flash / deepseek-v4-pro 的原价与低价时段折扣价两套，低价时段显示折扣价并标绿。
4. 今日/本月消费：DeepSeek 官方无消费查询接口，当前显示写死值/-- 是误导。改为：面板该区域显示「以控制台为准」并附控制台链接（点击 shell.openExternal 打开 https://platform.deepseek.com/usage）；本地统计 Tab 记录本插件查询到的余额变化推算「余额消耗」（最近 N 条记录差值），如实标注为推算值。
5. 余额变动提醒：本次查询与上次差值超过阈值（默认 $1，可在设置改）时弹气泡「余额消耗 $x.xx」；余额低于阈值时除气泡外，宠物切换 char_sleep + 气泡变红（现有逻辑）保持。
6. 刷新策略：设置页的刷新间隔增加「低价时段更勤快」选项——低价时段按设定间隔，高价时段自动 ×3 降频。

完成后：
- 填真实 Key 验证：正确 Key 出真实余额、错误 Key 出 401 提示、断网出网络异常提示、价格表随时段切换、余额消耗气泡
- 汇报：①修改了哪些文件 ②如何运行验证 ③完成了什么 ④尚未实现什么
不要为了完成任务而降低代码质量。
```

---

## 阶段四：工程化与发布（生产化）

```text
你现在是 DeepSeek Pet 桌面宠物项目的主程。项目位于 C:\Users\kgnb666.DESKTOP-55DCFH3.003\Desktop\DeepSeekPet，技术栈 Electron + 原生 JS。阶段一至三已完成。

硬约束：
- 不更改角色形象、UI 框架与对外功能行为
- 本阶段只做代码质量、安全性与发布物，不加新功能

必须实现：
1. 渲染层模块化：renderer.js 拆分为 src/modules/pet.js（状态机+动画）、bubble.js（气泡队列）、panel.js（面板+Tab）、menu.js（右键菜单）、drag.js（拖动）、api.js（IPC 封装），index.html 用 <script type="module"> 引入入口 src/renderer.js。拆完后行为与拆分前完全一致。
2. 代码规范：接入 ESLint（推荐配置）+ Prettier，package.json 加 lint / format 脚本，修复全部 lint 错误。
3. 安全加固：index.html 加 CSP meta（default-src 'self'; img-src 'self' data:; style-src 'self' 'unsafe-inline'）；preload.js 的 IPC 白名单加参数校验（如 win-move 的 dx/dy 必须为有限数值）。
4. GPU 策略：当前全局强制 disable-gpu。改为默认启用硬件加速，仅当检测到 GPU 进程崩溃（app.on('gpu-process-crashed')）时回退软渲染并提示用户；start-pet.bat 移除 --disable-gpu --no-sandbox。
5. 正式安装包：electron-builder 增加 nsis target（可选安装目录、桌面快捷方式、开始菜单、卸载入口、安装后可选开机启动），产出 DeepSeekPet-Setup-x.x.x.exe；portable 版保留。
6. 版本与更新：package.json 版本号升级，生成 CHANGELOG.md 记录 v1.0 → 当前版本全部变更；托盘「关于」对话框显示版本号。
7. README.md：项目说明、功能列表、截图占位、开发命令（npm install / npm start / npm run build）、目录结构说明。

完成后：
- npm run lint 零错误；安装包在干净 Windows 上安装→运行→卸载全流程验证；便携版同步产出
- 汇报：①修改了哪些文件 ②如何运行验证 ③完成了什么 ④尚未实现什么
不要为了完成任务而降低代码质量。
```

---

## 阶段验收对照表

| 阶段      | 核心产出   | 验收关键点                                        |
| --------- | ---------- | ------------------------------------------------- |
| 一 稳定性 | 无已知 bug | 位置恢复 / 单实例 / 状态持久化 / 时段提醒         |
| 二 体验   | 宠物"活"了 | 空闲行为 / 拖动不断 / 气泡队列 / 副屏 / 包体变小  |
| 三 数据   | 真实可用   | 加密存 Key / 错误分类提示 / 折扣价切换 / 消耗提醒 |
| 四 工程化 | 可发布     | 模块拆分 / lint 零错 / NSIS 安装包 / README       |
