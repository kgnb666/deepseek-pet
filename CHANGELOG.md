# Changelog

本项目的所有重要变更都会记录在此文件中。版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)（`主.次.修订`）。

## [2.7.0] - 2026-09-07

### 路线 A 落地：三段式序列帧管线（dsh-pet 同款无缝动画）

- **程序化序列帧生成器**（`tools/gen-anim-frames.py`，Pillow）：按交付规格为全部 14 个 FSM
  状态生成 PNG 序列帧——720×960 统一画布、脚底锚点 (360,900)、entry/loop/out 三段式、
  IDLE 48 帧呼吸循环、24fps、全透明背景；entry/out 用标准站姿与 loop 首帧做 alpha 交叉溶解，
  衔接完全隐形。接触阴影烘焙进每帧 + 「地板裁剪」（锚点以下像素清零）保证锚点逐帧零漂移。
- **交付校验全绿**：`node tools/validate-anim-assets.mjs` 748 帧 / 14 状态 / 0 问题
  （画布一致、RGBA、锚点行恒 900、帧号连续）。
- **三段式播放接入**（新模块 `src/modules/fsm/animPlayer.js`）：清单热加载
  `{fps, repeat, displayHeight, segments:{entry,loop,out}}`，播放链 上一状态 out →
  本状态 entry → loop×repeat（0=驻留）→ out，代际令牌防竞态；兼容旧 frames/sheet 清单。
  FrameAnimator 支持 displayHeight（帧画布 320px 显示，锚点与静态立绘基线对齐），
  播放时隐藏 CSS 地面阴影（帧内已烘焙）。
- **dsh-pet 垫片验证**（MIT 素材，仅验证不进包）：待机呼吸/漂浮踏步两段 WebM 经
  ffmpeg 抽帧 + 重锚转换后 482 帧全部通过校验——外部真实素材可跑通同一管线，上限确认。
- **运行体积**：清单运行时引用 WebP（约 90KB/帧）；PNG 源帧保留作交付/校验，
  打包通过 `!assets/animations/**/*.png` 排除。
- 修复：GOODBYE 终态时长 2000→1800（与文档/测试一致，告别淡出链 1.66s 完整播完）。
- 版本号 2.6.0 → 2.7.0。

## [2.6.0] - 2026-09-06

### 自动骨骼 rig：平设立绘的次要动作（对标 dsh-pet 的"活"感）

- **自动抠件**（新模块 `src/modules/rig.js`）：加载立绘后对 alpha 通道做连通域分析，
  自动识别与主体不相连的可动部件——呆毛（顶部小块）、鲸鱼跟班（左下中块）、尾巴（右侧大块）——
  抠成独立图层并给出挂载参数（偏移/底距/缩放），零手工标注。
- **独立次要动作**：呆毛 3.4s 弹簧摆动（±6°）、鲸鱼跟班 2.6s 自主呼吸起伏、尾巴 4.2s 慢摆；
  按下/撞墙时呆毛获得冲量弹跳（jolt）。图层随朝向翻转、随动画倍速缩放、非皮肤姿势自动隐藏。
- **四档弹弓**（对标 dsh-pet 甩出力度四档）：按拉弓距离分 4 档（×1.8/2.4/3.0/3.7），
  星星粒子数量与发射音效音高随档位提升。
- 运行验证：间隔连拍确认呆毛图层独立摆动、视觉无接缝无重影；67 测试全过。
- 说明：dsh-pet 的丝滑来自 97 段 24fps 预渲染 WebM（非 3D）；本项目已具备同等的
  WebM/雪碧图/序列帧热加载管线，手绘或 AI 生成帧序列放入 `assets/animations/` 即可自动接管播放。

## [2.5.0] - 2026-09-06

### 走路动画：告别「坐姿平移」，真实小碎步

- **表情移植立绘 `char_walk`**：把 char_happy 的开心笑脸椭圆区域移植到 char_angry 的站立身体上
  （同角色同画风，接缝不可见）——走路不再用坐姿滑行，也不至于怒脸散步。
- **四帧跳跃步态 `anim_walk`**：站立立绘底部锚定 ±5° 摇摆 + 14px 腾空 + 挤压拉伸，8fps 循环的
  动画 WebP（Shimeji 式 chibi 小碎步）；WALK 态自动播放，配合朝向翻转左右通行。
- wander.js 不再叠加 CSS 摇晃（避免与帧动画双重抖动）；生成脚本 `tools/gen-walk.ps1`。
- 运行验证：WALK 态截图确认站立步态帧 + 移动台词，窗口坐标持续变化确认游走正常。

## [2.4.3] - 2026-09-06

### 修复：右键菜单超出窗口高度显示不全

- **现象**：菜单加入「AI · 扩展」区后总高度（~640px）超过桌宠窗口高度（470px），定位逻辑又使用
  写死的 640px 常量把菜单顶出窗口上沿——顶部与底部条目同时被裁掉。
- **修复**：
  - 菜单内部可滚动（`max-height: calc(100vh - 12px)` + 细滚动条），任何条目都可达；
  - 定位改为实测 `offsetWidth/offsetHeight` 后动态钳制到窗口内（替换写死常量）；
  - 「演示 · 工作状态」6 项折叠为「展开演示」可展开组，菜单常规高度显著缩短。
- 运行时验证：右键截图确认全部条目可达、滚动条与折叠组工作正常。

## [2.4.2] - 2026-09-06

### 修复：GPU 回退后桌宠永久不可见

- **现象**：GPU 进程崩溃一次后，应用永久以软渲染启动，而透明无边框窗口在纯软渲染下整窗不可见
  （进程与余额轮询正常，但桌面什么都没有）。
- **根因**：GPU 回退标记 `gpuFallback` 是一次性写入、永不复位——用户被永久卡在不可见状态。
- **修复**：回退改为「安全模式」语义——崩溃后的下一次启动用软渲染运行，同时自动复位标记，
  再下一次启动恢复硬件加速；GPU 若再次崩溃则再次进入安全模式（自愈循环，不再永久卡死）。
- 已在现场复位标记并恢复显示（硬件加速下验证）。

## [2.4.1] - 2026-09-05

### FSM 专属姿势素材：从现有立绘程序化派生（全部就位）

- **困倦加载 `char_loading`**：char_sleep + 头顶阿基米德螺旋，8 帧旋转动画 WebP（LOADING 态自动播放）。
- **代码查错 `char_debug`**：char_work + 悬浮报错窗口（红色标题栏 + 高亮 bug 行 + ❌ 徽标）。
- **思考分析 `char_think`**：char_main + 三个倾斜问号。
- **报告完成 `char_success`**：char_happy + 五彩纸屑雨 + 绿色对勾徽章。
- **拒绝不听 `char_refuse`**：char_angry + 红边「No!」气泡（对齐设计稿第二集）。
- **挥手告别 `char_goodbye`**：char_happy（挥手）+ 蓝色行李箱。
- 生成脚本 `tools/gen-fsm-poses.ps1`（System.Drawing 绘制，ASCII 注释规避代码页陷阱）；
  立绘别名回退链自动启用新素材（THINKING/DEBUGGING/LOADING/REFUSE/SUCCESS/GOODBYE 不再降级到兜底姿势）。
- 运行时验证：LOADING 动画、DEBUGGING 驻留、REFUSE 气泡经 Agent 协议远程驱动并截图确认。
- 版本号 2.4.0 → 2.4.1。

## [2.4.0] - 2026-09-05

### 桌宠状态机管理系统（FSM）与帧动画控制器

#### 架构（`src/modules/fsm/`，核心纯逻辑可测试）

- **PetStateMachine**：有限状态机核心——四级优先级仲裁（基础态 < 互动 < 功能态 < 系统态 < 终态）、
  高优先级立即打断、临时态到期自动回退、IDLE 5s 到期按策略随机循环、终态锁定 + `exit` 事件；
  定时器/时钟可注入，14 个确定性单测（手动时钟）覆盖全部语义。
- **PetState**：14 态枚举（IDLE/WALK/REST/ROLL/STRETCH/PETTING/LOADING/CODING/DEBUGGING/THINKING/
  SUCCESS/ALERT/REFUSE/GOODBYE），元数据驱动（tier/kind/时长/回退）。
- **FrameAnimator**：帧动画控制器——序列帧（`<img>` 逐帧）与雪碧图（`#pet-sheet` 图层步进
  background-position）双模式，帧预加载 + 失败黑名单 + 暂停冻结。
- **PetBrain**：组合根——状态→立绘别名回退（专属素材热探测，素材就位免改码）、
  气泡台词、游走、特效、动画清单（`assets/animations/<STATE>.json` 热加载）、
  用户活动跟踪（点击宠物=摸头 PETTING，Hover 带 8s 冷却）、空闲 ≥60s 开启待机循环。

#### 集成重写

- **pet.js 拆除旧待机概率链**（30% 自语/15% 转身/35% 动作池/20% 游走的硬编码）——行为循环统一由
  FSM 驱动，权重来自生活状态引擎；心情/眨眼通过 `fsmVisualGate()` 感知 FSM，不再覆盖功能态立绘
  （修复：眨眼动画曾把远程驱动的 DEBUGGING 姿势打回皮肤）。
- **task.js(HUD) → FSM 桥接**：thinking→THINKING、working→CODING、success→SUCCESS、error→DEBUGGING；
  FSM 不回写 HUD，无回环。
- **panel.js**：余额查询网络错误 → REFUSE（拒绝不听）、其他错误 → DEBUGGING、余额不足 → ALERT（举快充值牌）。
- **menu.js**：「出去走走」走 FSM WALK（功能态时礼貌拒绝）；「退出」先播 GOODBYE 告别动画再退出。
- **Agent 协议 v2**：新增 `state` 事件直接驱动 FSM（`{"type":"state","value":"ALERT","duration":5000}`），
  CLI 支持 `node tools/agent-event.mjs state ALERT 5000`；主进程支持 `PET_PORT` 环境变量改端口
  （与已安装版共存测试）。

- 测试总量 53 → 67（+14 FSM 用例），全过；架构红线预算纳入 FSM 核心文件。

## [2.3.0] - 2026-09-05

### 面板气泡化：大幅瘦身，与台词气泡同视觉语言

- **气泡造型**：右侧面板从 440×470 的「窗口风卡片」改为 320 宽的圆角大气泡——毛玻璃背景、
  描边、左下角指向宠物的尾巴，与台词气泡/HUD 同一套设计语言。
- **头部两行化**：第一行 🐳 + 余额大字 + 操作按钮，第二行演示徽标 + 时段徽标；余额字号 30→21，
  价格/倒计时/统计等全部收紧字号与留白。
- **窗口同步瘦身**：主窗口 780×500 → 660×470，占地明显减小；迷你模式锚定逻辑同步更新。
- **信息零删减**：总览/统计/图表/设置四个页签与全部功能保留，设置页在气泡内滚动。
- 修复面板重构引入的多余 `</div>`（导致 Tab 栏脱离气泡被挤成竖排）。
- 版本号 2.2.0 → 2.3.0。

## [2.2.0] - 2026-09-05

### 生活状态引擎：桌宠自动识别当前状态并做出相应动作

- **状态融合（新模块 `src/modules/lifeState.js`，纯逻辑可测试）**：自动融合四个信号——
  DeepSeek 时段（低价/高价）、余额档位、是否深夜（23:00–6:00）、当前任务（thinking/working）——
  得出桌宠的「生活状态」，优先级：任务 > 深夜 > 余额告急 > 时段。
- **空闲动作偏好随状态变化**：低价时段爱打滚卖萌（roll 34%），高价/深夜蔫蔫地趴下打盹（lie 34–54%），
  余额告急时没心情玩（趴下 + 打盹为主），深夜几乎不动。动作池由硬编码概率改为引擎权重驱动。
- **周期性「状态举牌」**：空闲状态下每几分钟（带随机抖动防机械）自动举牌播报当前处境——
  低价时段举「低价！」牌、余额告急举「快充值！」牌、高价时段趴桌等降价、深夜趴桌催主人睡觉；
  任务进行中不打扰。此前这些反应只在状态切换瞬间触发一次。
- **开场姿势随状态**：启动问候时，低价时段直接举牌，深夜趴桌，高价打盹（此前只有气泡没有姿势）。
- 新增 10 个测试用例（状态优先级/权重分布/动作池完整性/举牌冷却/姿势对应），总 53 个全过。
- 版本号 2.1.0 → 2.2.0。

## [2.1.0] - 2026-09-05

### 派生素材：从现有立绘加工出新姿势（对标海报设计稿补齐缺口）

- **低价举牌 `char_sign`**：由 `char_main` + 圆角「低价！」举牌合成（System.Drawing 绘制 + 高质量变换），
  进入低价时段时桌宠举牌庆祝（替换原先的惊讶表情）。
- **余额告急 `char_alert`**：由 `char_surprise` + 红色「快充值！」举牌合成，余额跌破阈值时举牌提醒。
- **趴下休息 `char_lie`**：由 `char_sleep` 底部锚定压扁（×0.72）+ 微旋转派生，「瘫倒感」；加入空闲动作池。
- **打滚卖萌 `anim_roll`**：由 `char_happy` 旋转 ±12° 生成 8 帧动画序列，ffmpeg 编码为带透明通道的
  动画 WebP（318KB，10fps 循环）；加入空闲动作池（`<img>` 原生播放，无需视频解码）。
- **方形头像 `avatar`**：由 `char_happy` 面部裁切 256×256，迷你悬浮条与托盘使用（不再用整身立绘硬裁）。
- **生成脚本 `tools/gen-poses.ps1`**：全部派生素材可一键重新生成（含 PowerShell 5.1 GBK 代码页兼容处理）。
- 菜单「更换皮肤」加入 3 个新姿势；迷你条头像改用专属裁切。
- 版本号 2.0.0 → 2.1.0。

## [2.0.0] - 2026-09-05

### 对标 dsh-pet-indesktop 的全量追平版本

#### AI 能力

- **AI 聊天工作台（新窗口 `chat.html`）**：多会话管理（置顶/重命名/搜索/批量删除/清空）、SSE 流式输出、多 Provider（任意 OpenAI 兼容服务，Key 走 safeStorage 加密）、自定义 System Prompt、跟随桌宠移动、四套主题、Markdown-lite 渲染（代码块/行内代码/粗体）、复制消息。
- **快速对话气泡**：桌宠头顶迷你输入框（右键菜单「💬 快速对话」），Enter 发送，回答流式写回台词气泡。
- **看看屏幕**：截屏 + 前台窗口上下文 → 视觉模型（任意配置了 visionModel 的 Provider），截图仅内存不落盘。
- **主动识屏陪伴**：轮询前台窗口（PowerShell），切到白名单应用时主动关怀冒泡，默认关闭；白名单支持子串与 `*` 通配（`src/shared/screenMatch.mjs`）。
- **Agent 联动协议 v1**：本地 HTTP 服务（127.0.0.1:27890，可配 Token）接收 `task / say / chat / mood / pose / think` 事件（JSON / 数组 / JSONL），支持鉴权；CLI 发送器 `tools/agent-event.mjs`、Claude hooks 示例 `integrations/claude-hooks.example.json`、协议文档 `docs/AGENT_PROTOCOL.md`。Agent 事件带联动音效（音效包 `agent` 事件）。

#### 扩展玩法

- **多实例多开**：托盘/右键「🐣 生一只小猫」；`--pet-name=xxx` 独立 userData 配置隔离，单实例锁按 userData 区分。
- **多开碰撞物理**：UDP gossip（端口段 27891–27896，最多 6 只猫）+ AABB 弹性碰撞（`src/shared/collisionPhysics.mjs`），空闲/游走/飞行中都能被撞飞，「咚」一声 + 惊讶表情 + 冲量叠加。
- **角色热加载**：`%APPDATA%/deepseek-pet/characters/<id>/character.json` 声明姿势素材，支持 alpha WebM/MP4 视频（`<video>` 播放）与 PNG/WebP/GIF；fs.watch 热加载；右键「更换皮肤」循环内置 + 外部角色；模板 `docs/character.template.json`。
- **自定义音效包**：`sounds/<event>.mp3|wav|ogg|flac` 覆盖 12 类事件音效，热加载，缺省回退合成音。
- **余额分档心情**：6 档（欠费/弹尽粮绝/细水长流/小有富余/腰缠万贯/鲸鱼大户），跨档切姿势 + 台词。
- **音乐检测自动唱歌**：系统声音 loopback（Windows）RMS 阈值判定，摇摆 + 歌词气泡，默认关闭。
- **检查更新**：GitHub Releases 优先 + jsDelivr `package.json` 兜底（`electron/updater.js`），托盘与右键入口。
- **系统通知**：任务成功/失败可弹 Windows Toast（30s 去重）。
- **全屏自动隐藏**：区分真全屏与最大化，全屏时隐藏、退出恢复。
- **OBS/捕获**：默认可被窗口捕获；新增「防截屏捕获」开关（`setContentProtection`）。
- **彩蛋**：连续快速点击 9 次触发「欧鲸鲸」鲸鱼雨；投喂/彩蛋/陪唱统计入统计页。
- **台词分页**：超长气泡按行分页显示（对标 dsh-pet 自言自语分页），带续页指示。

#### 桌宠本体增强

- 动画播放倍速 1.0–2.0x（设置滑杆，作用于呼吸/走路/说话等循环动画）。
- 不透明度下限放宽至 10%。

#### 工程化

- **测试套件**：`npm test`（node:test）43 个用例：时段判断 / Agent 协议 / 碰撞物理 / 余额分档 / semver / 白名单匹配 / 会话存储 + **架构红线测试**（核心文件行数预算、共享模块纯净性、IPC 白名单一致性）。
- **GitHub Actions CI**：Ubuntu + Windows 双平台 lint + test；打 tag 自动构建 portable + NSIS 并上传产物。
- **架构重组**：主进程子系统拆分到 `electron/`（agentLink/llm/providers/chatStore/screen/collision/updater/hotAssets）；跨端纯逻辑抽到 `src/shared/*.mjs`（渲染层 ESM / 主进程动态 import / 测试三端复用）。
- 共享逻辑去重：时段判断统一到 `src/shared/offpeak.mjs`。

## [1.10.0] - 2026-09-05

### 桌宠交互对标 dsh-pet-indesktop（按下反馈 / 抓起姿态 / 弹弓 / 自主游走）

- **按下即时反馈**：pointerdown 立即 Q 弹压扁 + 轻音（`petOnPress`），之后由位移阈值区分「确认点击」（分部位反馈）与「拖拽」（抓起反馈），手感对标 dsh-pet 的 press/click 两段式音效。
- **抓起姿态**：拖过阈值切 `char_surprise` + 悬空扑腾动画 + 抓起音效；拖拽中宠物朝向随手方向翻转（新增 `--flip` 朝向系统，默认朝右）。
- **弹弓蓄力**：拖拽中按住右键蓄力（后仰瞄准动画），松左键 ×2.6 倍发射（几乎静止时朝面向斜上方弹出）；蓄力期间右键菜单自动屏蔽。
- **自主游走（新模块 `src/modules/wander.js`）**：空闲时随机走向当前显示器 workArea 内目标点，走路摇晃 + 朝向目标，到达后东张西望；任意交互立即打断；锁定位置 / 迷你 / 暂停 / 闹脾气时不游走。右键菜单新增「🚶 出去走走」手动触发。
- **待机概率链重构**（对标 dsh-pet 动画状态机）：自语气泡 30% / 转身（东张西望，新行为）15% / 动作池（打盹/卖萌/伸懒腰）35% / 游走 20%。
- **闲置降帧**：30 秒无交互后 bob 呼吸动画降速一半省电，任意交互恢复。
- 版本号 1.9.0 → 1.10.0。

## [1.9.0] - 2026-09-05

### 桌面体感（对标 dsh-pet）

- **鼠标穿透**：开启后空区点击落到下层窗口；角色、面板、气泡、右键菜单仍是可点热区。
- **锁定位置**：锁定后不可拖动；按住 Shift 仍可临时拖动。
- **不透明度**：设置页滑杆 30–100%，实时生效并持久化。
- **隐藏省电**：窗口隐藏时暂停余额刷新与空闲行为，显示后恢复。
- **余额缓存**：30 秒内重复查询直接返回缓存，降低 API 频率。
- 修复 1.8.0 漏掉的 `#task-hud` DOM，工作状态 HUD 现在能正常渲染。
- 版本号 1.8.0 → 1.9.0。

## [1.8.0] - 2026-09-05

### AI 工作状态气泡

- 新增独立 Task HUD（`#task-hud`），不替换原有台词气泡。
- `TaskManager`（`src/modules/task.js`）：`idle / thinking / working / success / error`，统一入口 `updateTask()`。
- 进行中任务 HUD 常驻并刷新进度/已运行时间；success/error 停留 3 秒后淡出。
- 立绘联动：思考冒泡、工作轻晃、成功撒花、失败抖动。
- DeepSeek 模板：`taskDeepSeekCall` / `taskBalanceQuery` / `taskOffpeak`。
- 手动刷新余额会走 working → 完成/失败 HUD。
- 测试：右键「演示 · 工作状态」，或 DevTools 调 `window.updateTask({...})`。
- 版本号 1.7.1 → 1.8.0。

## [1.7.1] - 2026-09-05

### 交互补漏

- 面板「切换模型」链接实际绑定事件（此前 HTML 有入口、JS 未监听）。
- 右键菜单「迷你模式」补上 `case 'mini'`（此前点了没反应）。
- 说话嘴型：气泡展示时给 `#pet-wrap` 加 `talking` class，结束时移除。
- 迷你条时段徽标随 `tickPeriod` 更新（高价/低价不再写死）。
- 演示数据不再写入余额历史，避免污染折线图。
- 托盘「打开面板」在迷你模式下会先退出迷你。
- `app://` 路径穿越防护改为 `path.resolve` + 目录分隔符前缀匹配。
- 版本号 1.7.0 → 1.7.1。

## [1.7.0] - 2026-09-05

### 交互精品化（补齐二三档剩余项）

- **拖拽物理**：松手速度足够则惯性抛掷，撞到屏幕边缘反弹并旋转；轻拖仍只回弹。
- **新立绘表情**：生气 / 害羞 / 惊喜 / 眨眼（WebP+PNG），接入心情、双击、眨眼、余额反应与换肤循环。
- **说话嘴型**：气泡出现时 `#mouth` CSS 开合，与说话同步。
- **节日/天气氛围**：元旦/情人节/劳动节/儿童节/国庆/平安夜/圣诞 + 春节/中秋近似窗口；无天气 API 时用时段/季节台词兜底。
- **余额深度联动**：骤降惊慌/大额消耗生气、余额增加庆祝、低于阈值强烈警告、余额充足得意撒花。
- 版本号 1.6.0 → 1.7.0。

## [1.6.0] - 2026-09-04

### 交互全面升级

- **悬停 + 视线跟随**：鼠标移上宠物轻微放大；朝指针方向轻微倾斜（`#pet-wrap` 承载交互 transform，不破坏立绘 bob/pat 动画）。
- **分部位点击**：点头 / 摸肚子 / 拉尾巴不同音效、台词与动作。
- **双击摸头**：开心加倍（爱心+星星+弹跳）。
- **连点连锁**：1 秒内连点 5 次触发转圈眩晕 + 特殊台词。
- **心情状态机**：开心 / 无聊 / 犯困 / 傲娇。长时间不理会背对闹脾气，点击安抚。
- **好感度**：跨会话累计，统计页可见；10/30/50/100/200 里程碑彩蛋。
- **投喂玩法**：右键菜单「🐟 投喂小鱼干」，吃食动画 + 音效 + 好感度。
- **拖拽松手**：回弹抖动。
- **多音效**：摸头/肚子/尾巴、双击、连锁、投喂、换肤、起床、休眠、警告、庆祝。
- **余额联动**：余额骤降惊慌抖动 + 警告音；低价时段开始撒花庆祝。
- 版本号 1.5.0 → 1.6.0。

## [1.5.0] - 2026-09-04

### 交互与音效增强

- 新增**点击互动音效**：用 Web Audio API 实时合成轻快提示音（软"啵" + 上扬"叮"），无需外部音频文件，音高带轻微随机化避免单调。
- **点击互动增强**：点击宠物除歪头、爱心、随机台词外，随机触发星星/闪光粒子，反馈更丰富。
- 新增**音效开关**（右键菜单），状态持久化到设置（默认开启）。
- 版本号 1.4.1 → 1.5.0。

## [1.4.1] - 2026-09-04

### 数据与版本对齐

- **对齐官方模型与价格**：模型由旧名 `deepseek-chat` / `deepseek-reasoner` 切换为 DeepSeek 官方现行模型 `deepseek-v4-flash` / `deepseek-v4-pro`；价格表更新为官方 USD 计价（每 1M tokens）：
  - **V4 Flash**：原价 缓存命中 $0.014 / 未命中 $0.44 / 输出 $1.32；低价时段半价。
  - **V4 Pro**：原价 缓存命中 $0.044 / 未命中 $1.32 / 输出 $3.96；低价时段半价。
  - 来源：[DeepSeek API 官方定价页](https://api-docs.deepseek.com/quick_start/pricing/)。
- **货币符号（¥）**：按主人要求，价格表与账户余额、阈值、变动提示、图表坐标统一显示 `¥`（人民币符号）；模型价格数值采用 DeepSeek 官方 USD 口径原价（未做汇率换算，上方 `$` 数值即官方原价）。
- 版本号 1.4.0 → 1.4.1。

## [1.4.0] - 2026-09-04

### 工程化与发布（阶段四）

#### 代码模块化

- 将单文件 `renderer.js` 拆分为 `src/` 目录下的 ES Module 模块：`state`（共享状态/常量）、`api`（IPC 封装）、`bubble`（气泡队列）、`pet`（宠物状态机）、`panel`（面板/余额）、`menu`（右键菜单）、`drag`（拖动）。
- `index.html` 改用 `<script type="module" src="src/renderer.js">`。
- 新增 `app://` 自定义协议托管本地资源（解决 `file://` 下 ES Module 被 CORS 拦截的问题），并内置路径穿越防护。

#### 代码质量

- 引入 ESLint（flat config）+ Prettier，新增 `npm run lint` / `npm run format` 脚本，lint 零错误。
- 统一代码风格（单引号、分号、LF 换行、110 列宽）。

#### 安全加固

- `index.html` 新增 CSP：`default-src 'self'; img-src 'self' data:; style-src 'self' 'unsafe-inline'`。
- `preload.js` IPC 白名单增加参数校验：`saveSettings` 仅接受纯对象，`moveWindow` 的 `dx/dy` 必须为有限数值。

#### GPU 策略

- 默认启用硬件加速；仅在 GPU 进程崩溃时（`app.on('gpu-process-crashed')` / `child-process-gone`）写入回退标记并重启，下次以软渲染启动。
- 移除 `start-pet.bat` 中的 `--disable-gpu --no-sandbox` 参数。

#### 发布

- electron-builder 新增 `nsis` 目标（可选安装目录、桌面快捷方式、开始菜单、卸载、可选开机自启），`portable` 版保留。
- 托盘菜单新增「关于」对话框，显示当前版本号。

#### Bug 修复

- **优惠时段判断（严重错误）**：原代码把低价时段写成单连续区间 `00:30–08:30`，与 DeepSeek 官方定义完全不符。已改为官方规则——**仅工作日（周一~周五）UTC 01:00–04:00 与 06:00–10:00 为高价，其余时间（含周末全天、工作日午间 12:00–14:00、夜间 18:00–次日 09:00）均为低价半价**（北京时间：工作日 09:00–12:00、14:00–18:00 高价）。同步修正面板文案、右键价格信息文案、README 时段说明，并用多时间点单测验证。

## [1.3.0] - 2026-09-04

### 数据落地（阶段三）

- API Key 改用 Electron `safeStorage` 加密落盘，不可用时降级明文并标记；支持明文 Key 自动迁移为密文。
- 余额查询错误分类（认证失败 / 请求过频 / 网络异常 / 其他），不再静默降级。
- 价格表外置到 `prices.json`，缺失时使用内置默认价并提示。
- 余额变动提醒、低价时段降频刷新（高价时段 ×3 间隔）、消费明细跳转 DeepSeek 控制台。

## [1.2.0] - 2026-09-04

### 体验优化（阶段二）

- 新增暂停动画、空闲随机行为（眨眼 / 发呆 / 拉伸 / 随机台词）。
- 拖动改用 PointerCapture；气泡队列（排队不覆盖）；多显示器适配；素材 WebP 化（PNG 兜底）。

## [1.1.0] - 2026-09-04

### 稳定性（阶段一）

- 单实例锁（双开聚焦已有窗口）；面板开关状态持久化；窗口位置保存/恢复；托盘菜单；时段切换提醒；余额错误徽标。

## [1.0.0] - 2026-09-03

### 初始版本

- 深蓝色系猫耳少女桌宠形象；DeepSeek API 余额实时监控；低价时段提醒。
